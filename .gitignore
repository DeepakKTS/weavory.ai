node_modules/
dist/
coverage/
.env
.env.*
*.log
*.tsbuildinfo

# LanceDB / DuckDB local data
.weavory-data/
*.duckdb
*.duckdb.wal

# Generated dashboard data (but keep the dashboard HTML itself)
ops/data/*.json
!ops/data/.gitkeep
# gates.json is a committed audit trail of passed gates — not transient
!ops/data/gates.json
# bench.json is a committed baseline of local throughput numbers — regenerate
# via `pnpm bench`; committed so the dashboard has real values on checkout
!ops/data/bench.json
# Adversarial drill artefacts (Phase G.3) — transient, regenerated per run
ops/data/incidents/

# Editor artefacts
.DS_Store
.vscode/
.idea/

# Keys (we never commit signer keys)
*.pem
keys/
